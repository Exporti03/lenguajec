#include <iostream>

int main(){

// Operadores terminados ? = es un remplazo para usar una declaracion if

bool hambriento = false;

//numero % 2 ? std::cout << "impar" : std::cout << "par";

hambriento == true ? std::cout << "tengo hambre" : std::cout << "no tengo hambre";

    return 0;
}