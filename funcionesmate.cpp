#include <iostream>
#include <cmath>

int main() {
    double x = 3.5;
    double y = 4;
    double z;

    //z = std::max(x,y);
    //z = std:: min(x,y);
    //z = std::pow(2,4);
    //z = std::sqrt(9);
    //z = std::abs(-3);
    //z = std::round(x);
    //z = std::ceil(x);
    //z = std::floor(x);


    std::cout << z;

    return 0;
}